<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="TabelaFipe.Veiculo">
    <ContentPage.Content>
        <StackLayout>
            <Label Text="Informações do Veículo" x:Name="lblMsg"
           HorizontalOptions="Center" Margin="40"/>
            <Label Text="{Binding name}"
                   VerticalOptions="Start"
                   HorizontalOptions="CenterAndExpand"
                   FontSize="18" FontAttributes="Bold" />
            <Label Text="{Binding marca}"
                   VerticalOptions="Start"
                   FontSize="18" HorizontalOptions="CenterAndExpand" />
            <Label Text="{Binding ano_modelo}"
                   VerticalOptions="Start"
                   FontSize="18" HorizontalOptions="CenterAndExpand" />
            <Label Text="{Binding combustivel}"
                   VerticalOptions="Start"
                   FontSize="18" HorizontalOptions="CenterAndExpand" />
            <Label Text="{Binding preco}"
                   VerticalOptions="Start"
                   FontSize="18" HorizontalOptions="CenterAndExpand" />
        </StackLayout>
    </ContentPage.Content>
</ContentPage>